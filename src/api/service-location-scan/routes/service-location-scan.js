'use strict';

/**
 * service-location-scan router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::service-location-scan.service-location-scan');
